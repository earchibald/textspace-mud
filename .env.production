# Production Environment Configuration for Railway

# Backend Selection
USE_DATABASE=true

# Database Configuration (Railway persistent volume)
DATABASE_PATH=/app/data/textspace.db

# Security (Railway will provide these via environment variables)
SECRET_KEY=${SECRET_KEY}
PASSWORD_SALT_ROUNDS=12

# Server Configuration
HOST=0.0.0.0
TCP_PORT=8888
WEB_PORT=5000
